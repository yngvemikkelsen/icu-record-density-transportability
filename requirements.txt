# Dependencies for the analysis scripts in scripts/.
#
# The scripts carry PEP 723 inline metadata and are intended to be run with uv,
# which resolves these automatically:
#
#     uv run scripts/paper17_diagnose_exposure.py --help
#
# This file exists for anyone using a conventional virtual environment instead:
#
#     python -m venv .venv && source .venv/bin/activate
#     pip install -r requirements.txt
#
# Lower bounds are the versions the analysis was developed and run against.
# No upper bounds are pinned: nothing here uses an API that has been unstable
# across these ranges. If you need byte-exact reproduction, pin the exact
# versions recorded below.

pandas>=2.2
numpy>=1.26
statsmodels>=0.14
pyarrow>=15.0        # parquet caches for the extracted timestamp series
matplotlib>=3.8      # figures only (scripts/paper17_figures.py)

# Versions used for the results reported in the manuscript:
#   pandas 3.0.2, numpy 2.4.4, statsmodels 0.14.6, pyarrow 25.0.1,
#   matplotlib 3.10.8, Python 3.12
